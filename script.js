function showNote() {
  alert("Glad for the moments, the conversations, and the time we share 🌷");
}

const overlay = document.getElementById("overlay");

overlay.addEventListener("click", () => {
  overlay.classList.add("hide");
});
